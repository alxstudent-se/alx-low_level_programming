#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main () {
   char ch;

   for(ch = 'a' ; ch <= 'e' ; ch++) {
      putchar(ch);
   }
   for(ch = 'f' ; ch <= 'z' ; ch++) {
	putchar(ch);
	}
   return(0);
}
